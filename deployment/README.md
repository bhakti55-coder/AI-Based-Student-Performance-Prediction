# Deployment
This folder contains deployment files like Dockerfiles, deployment scripts, API configurations, and hosting instructions.

This directory contains the assets required to deploy the AI model into a live application.

* **`student_model.pkl`**: The serialized Linear Regression model (saved via Python's `pickle` library). This file allows the pre-trained AI to make instant predictions in a web application without needing to be retrained.